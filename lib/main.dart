import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Center( // this widget is usd for center the content
        child: Text('hello world'), // text is using to add text content
        ),
        ),
        );
}

